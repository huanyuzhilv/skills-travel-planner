# 路书交付环境变量（复制为 .env 放在仓库根目录，与 scripts/ 同级）
# deliver_roadbook_v2.py 与 enrich_daily_descriptions_from_xhs.py 启动时会自动加载本文件；
# 已在 shell 里 export 的同名变量不会被覆盖。
#
# ── 小红书配图 / 正文（推荐）──
# TIKHUB_API_KEY=          # https://user.tikhub.io 申请
#
# ── 每日 description LLM（二选一；同时设置时优先 OPENAI_API_KEY）──
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
#
# DeepSeek 专用（不设 OPENAI_API_KEY 时生效）
# DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# DEEPSEEK_MODEL=deepseek-chat
#
# 说明：见仓库 docs/deepseek-llm-setup.md
